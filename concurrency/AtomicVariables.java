package concurrency;

/**
 * A java.util.concurrent.atomic.* package provides
 * operations on underlying value that can be read and written atomically,
 * and also contains advanced atomic operations.
 *
 * It have get and set methods that work like reads and writes on volatile variables.
 * That is, a set has a happens-before relationship with any subsequent get on the same variable.
 *
 * The atomic compareAndSet method also has these memory consistency features.
 */
public class AtomicVariables {

}
